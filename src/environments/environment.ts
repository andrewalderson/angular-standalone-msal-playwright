export const environment = {
  // in real world scenarios this url may or may not be different for each environment.
  // We could have a different endpoint for each environment or we could use the same url
  // and update the msal.config.json file in a CI/CD pipeline when deploying to different environments
  msalConfigUrl: 'msal.config.json',
};
